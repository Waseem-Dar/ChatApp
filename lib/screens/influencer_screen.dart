import 'package:flutter/material.dart';

class InfluencerScreen extends StatelessWidget {
  const InfluencerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Influencer Screen"),),

    );
  }
}
